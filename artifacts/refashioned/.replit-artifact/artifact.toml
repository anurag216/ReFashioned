kind = "web"
previewPath = "/"
title = "RE:Fashioned"
version = "1.0.0"
id = "artifacts/refashioned"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24650

[services.development]
run = "pnpm --filter @workspace/refashioned run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/refashioned", "run", "build" ]
publicDir = "artifacts/refashioned/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24650"
BASE_PATH = "/"
